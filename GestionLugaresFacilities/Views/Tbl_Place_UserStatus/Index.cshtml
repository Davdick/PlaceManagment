@model IEnumerable<GestionLugaresFacilities.Models.Tbl_Place_UserStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.statusU)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.statusU)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_status }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_status }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_status })
        </td>
    </tr>
}

</table>
