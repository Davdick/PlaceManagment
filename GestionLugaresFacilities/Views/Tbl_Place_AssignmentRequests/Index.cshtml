@model IEnumerable<GestionLugaresFacilities.Models.Tbl_Place_AssignmentRequests>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tbl_Place_Code.data_coord)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tbl_Place_Users.id_employee)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Place_Code.data_coord)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tbl_Place_Users.id_employee)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_assreq }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_assreq }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_assreq })
        </td>
    </tr>
}

</table>
